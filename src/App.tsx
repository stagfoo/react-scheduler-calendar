import 'antd/dist/antd.css';
import React from 'react';
import ReactDOM from 'react-dom';
import ReactSchedulerCalendar from 'example/src/example';

ReactDOM.render(
  <ReactSchedulerCalendar/>,
  document.getElementById('root'),
);
